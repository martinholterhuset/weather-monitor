name: Værvarsel Monitor

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  weather-check:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'
    
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
    
    - name: Run weather monitor
      env:
        SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        USER_EMAIL: ${{ secrets.USER_EMAIL }}
        THRESHOLD_PRECIPITATION_HOURLY: ${{ vars.THRESHOLD_PRECIPITATION_HOURLY || '5.0' }}
        THRESHOLD_PRECIPITATION_DAILY: ${{ vars.THRESHOLD_PRECIPITATION_DAILY || '30.0' }}
        THRESHOLD_TEMPERATURE_SWING: ${{ vars.THRESHOLD_TEMPERATURE_SWING || '15.0' }}
      run: |
        python weather_monitor.py

